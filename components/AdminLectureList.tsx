// This component is deprecated — lectures are now managed directly as Chapters.
// Kept as a stub to avoid import errors during migration.
export default function AdminLectureList() {
  return null
}

